# Rust
/target
**/*.rs.bk

# Release packaging output (scripts/package-release.sh)
/dist-release

# Node / frontend (frontend/dist is a build artifact, embedded at compile time)
node_modules
frontend/dist

# Local env / secrets
.env
*.local
.cargo/config.toml

# SDD progress ledger + scratch (skill-managed, local only)
.superpowers/
